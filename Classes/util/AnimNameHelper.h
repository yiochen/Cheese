#pragma once
#include "cocos2d.h"
#include <string>
#include <sstream>
#include "common_include.h"
USING_NS_CC;
using namespace std;
namespace anim_name {
	std::string directionalString(std::string prefix, Vec2 direction);
}